//assignments
const country = "Armenia";
const continent = "europe";
const isIsland = false;
const language = 'Armenian';
const population = 3000000;
const finlandPopulation = 6000000;
const avrgpop = 33000000;
const speaksEnglish = false;

// console.log(`${country}'s population is ${population > 33000000 ? `above` : `below`} average`);

/*
switch(language){
    case 'Mandarin':
    case "Chinese":
        console.log(`very niiice🦧`);
        break
    case 'Spanish':
        console.log(`si si🧦`);
        break
    case 'English':
        console.log(`botah of watah 🧪`);
        break
    case 'hindi':
        console.log(`Jeans tnglnglnlglglgnl✈`);
        break
    default:
        console.log(`wow🌋`);    
}
*/

/*
if (language === 'English' && population < 50000000 && !isIsland){
    console.log(`${country} is a good place to be for you`);
} else {
    console.log(`${country} is not a great place for you,sry.`);
}

if (population > avrgpop){
    console.log(`Armenia's population is above average`);
} else {
    const difference = avrgpop - population;
    console.log(`Armenia's population is ${difference} below average`);
}
*/

/*
const nmbNeighbors = Number(prompt('How many neighbors does your country have?'));

if (nmbNeighbors === 1){
    console.log(`1 border huh?cool,cool..`);
} else if (nmbNeighbors > 1){
    console.log(`you got more borders than some..`);
} else {
    console.log(`you're prbbly on an island,,cool..`);
}
*/

/*
console.log(country);
console.log(continent);
console.log(population);
console.log(language);
console.log(typeof isIsland);
console.log(typeof population);
console.log(typeof country);
console.log(typeof language);
console.log (population);
console.log(population>=finlandPopulation);
console.log(population<avrgpop);
*/
