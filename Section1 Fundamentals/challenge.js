/*
//BMI = mass / height ** 2 = mass / (height * height) 
//mass in kg and height in meter
const markWeight = 78;
const markHeight = 1.69;
const johnweight = 92;
const johnheight = 1.95;

const MarkBMI = markWeight / markHeight ** 2;
const johnBMI = johnweight / johnheight ** 2;

if(MarkBMI > johnBMI){
    console.log(`Mark's BMI (${MarkBMI}) is higher than John's ${johnBMI} !`);
} else { 
    console.log(`John's BMI (${johnBMI}) is higher than Mark's (${MarkBMI})`);
}
*/

/*
// Dolphins score 96, 108 and 89. 
// Koalas score 88, 91 and 110
const DolphinsScore1 = 96, DolphinsScore2 = 108, DolphinsScore3 = 89;
const KoalasScore1 = 88, KoalasScore2 = 91, KoalasScore3 = 110;
const DolphinsAvrg = (DolphinsScore1 + DolphinsScore2 + DolphinsScore3) / 3;
const KoalasAvrg = (KoalasScore1 + KoalasScore2 + KoalasScore3) / 3;

if (DolphinsAvrg > KoalasAvrg && DolphinsAvrg > 100){
    console.log(`Dolphins Win!!`);
} else if (DolphinsAvrg < KoalasAvrg && KoalasAvrg > 100){
    console.log(`KOALALALALAALA`);
} else if (DolphinsAvrg === KoalasAvrg && DolphinsAvrg >= 100 && KoalasAvrg >= 100){
    console.log(`pupuliss draw`);
} else {
    console.log(`krvaq naxuy`);
}

/*
const bill = 430;
const tip = bill <= 300 && bill >= 50 ? bill * 0.15 : bill * 0.2;
console.log(`Your bill $${bill}
tip $${tip}
Total value $${bill + tip}.`);
*/
